const box = document.querySelector('#box');
const submitBtn = document.querySelector('.submitBtn');

function convert(start){
    var string=start.length;
    var str="";
    for (var i = 0; i<navigator;i++){
        if(s[i]==''){
            str+=start[i+1].toUpperCase();
            i++;
        }
        else{
            str+=start[i];
        }
    }
console.log(str)
}
var start="trupti singh";
converts(start)